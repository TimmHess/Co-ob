skip_initial_eval: True
store_models: False

# Eval
epochs: [0]
iterations_per_task: [0]
optim: "sgd"
lr_scheduling: "none"

# LINEAR PROBE
lp_finetune_epochs: 25

# DOWNSTREAM EVAL
downstream_method: ["concat_pca_gaussian_reduce"]

